#include <stdio.h>
main() 
{
	int i,toplam=0;
	float ortalama=0;
	for (i=1;i<100;i++);
	toplam = toplam+i;
	ortalama = toplam/100;
	{
	printf("toplam: %d\n", toplam);
	printf("ortalama: %f", ortalama);
    }
	return 0;
}
